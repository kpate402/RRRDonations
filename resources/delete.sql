-- Drop the donors table if it exists
DROP TABLE IF EXISTS donors;

-- Drop the beneficiaries table if it exists
DROP TABLE IF EXISTS beneficiaries;

-- Drop the non_profit_organizations table if it exists
DROP TABLE IF EXISTS non_profit_organizations;